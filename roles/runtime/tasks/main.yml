---

- name: Update apt cache
  ansible.builtin.apt:
    update_cache: true
    cache_valid_time: 3600

- name: Install Python runtime packages
  ansible.builtin.apt:
    name: "{{ runtime_python_packages }}"
    state: present
